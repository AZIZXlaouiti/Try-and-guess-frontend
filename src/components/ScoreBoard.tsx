import React from 'react'
import { useSelector } from 'react-redux'
export const ScoreBoard: React.FC = ()=>{
  // const players = useSelector((state:any)=> state.sessions.current)
  
    return (
        <div id='scoreboard'>
           {/* {players.map((e,i)=><h2 key={i}>{e.user.username}</h2>)} */}
        </div>
    )
}